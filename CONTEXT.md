# Test Lab

多机测试与运行期监控工具的词汇表。这里只有词的定义，没有实现细节。内核层的词不带
任何被测系统的概念；被测系统自己的词在它的适配层里定义。

## Language

### 内核

**Lab**:
一套让 agent 指挥多台机器上的被测系统的工具，两种模式：跑场景产出证据，或持续观察
正在运行的系统、检出异常并修复。
_Avoid_: 测试平台、TestLab.Agent、框架

**Agent**:
驱动 lab 的 AI 一方，例如一个 Claude Code 会话。lab 里没有叫 agent 的常驻进程。
_Avoid_: LabAgent、runner、bot

**Machine**:
lab 能到达的一台计算机，用登记册里的名字指代，从不用主机名或 IP。远程机器的名字就是
它的 ssh 别名；`local` 保留给 lab 进程所在的这台机器，它从不解析成别的名字。
_Avoid_: 节点、主机、PC、车

**Transport**:
传输。lab 到达一台机器并在它上面执行钩子的那一层。它自己的失败——连不上、连到一半断了
——与被测系统的失败是两类事，证据里必须分得开。传输可以重连，动作不会因此重做。
Run 下传输断得太久就是 run 失败；**Watch 下不是**，那里断连只是一段标注，见 Probe。
_Avoid_: 连接、通道、ssh（那是它今天的实现）

**Registry**:
机器登记册。每台机器一条记录：名字、怎么到达、属于哪个 tier、机器能力与标签。登记册
只声明能力，从不出现角色名；一个角色能不能落在一台机器上，是绑定时校验出来的。
_Avoid_: 机器池、inventory、fleet、在登记册里列角色

**Tier**:
分层。一台机器是实验室的还是生产的。生产机器永远不会被缺省绑定，必须在 run 里点名。
_Avoid_: 环境、env、用 prod/dev 布尔代替

**Binding**:
绑定。一次 run 里角色到机器的映射，由 agent 显式给出，没给出的角色落在 `local`。内核
只校验绑定是否成立，不替 agent 挑机器。
_Avoid_: 调度、placement、部署拓扑

**Cast**:
阵容。一个场景要的角色实例集合：哪些角色、每个几份、实例叫什么。由场景自己声明，
不声明就是每个角色一个实例。它说的是「要几份」，binding 说的是「在哪台机器上」，
两件事分开——同一个阵容可以绑到实验室的两台 VM 上，也可以绑到两台真车上。
_Avoid_: 拓扑、实例数、编队、与 binding 混为一谈

**Role**:
适配层交给 lab 管理的一种进程在 lab 眼中的身份，例如服务端、客户端、模拟器、假 RIoT。
被测进程与替身都是角色，用 kind 区分：`sut` 是被测系统自己的进程，`double` 是适配层为
测试提供的替身，`external` 是这套装置里 lab 管不着的东西——一台第三方硬件、一个别人在运维的
服务。lab 不部署也不启停 external，它没有代码所以钉不了 commit，但 lab 可以旁听它、可以拿它
判不变量。一台机器可以承担多个角色。角色只声明它对机器的需求，不指定机器。同一个角色可以有多个
**实例**，用 `<角色>/<实例名>` 指代——两台车就是 `onboard-hmi/a` 与 `onboard-hmi/b`；
只有一个实例时它的名字是 `default`，可以省略不写。
_Avoid_: 组件、服务、节点类型、替身另立一套词、把实例说成一台机器、给 external 派活

**Adapter**:
把一个被测系统接进 lab 的那层：它的角色清单、每个角色怎么发布、落地、启动、判就绪、
停止，控制面在哪，日志在哪，场景库在哪。
_Avoid_: 插件、profile、driver

**Scenario**:
一次有明确判据的编排：准备状态、驱动、等待、断言。lab 的最小执行单位。
_Avoid_: 测试、用例、test case、journey

**Run**:
一个场景的一次执行，有全局唯一的 `runId`，从开始到证据落盘。有终点。结局有三种：
`PASS`、`FAIL`、`ERROR`——`ERROR` 是编排或传输的故障，不是被测系统的判定，所以可以
重跑；`FAIL` 是被测系统的判定，不许重跑覆盖。持续观察一套已经在跑的系统不是 run，
见 Watch。
_Avoid_: 执行、trial、session、用它兼指一段观察、把传输故障算成红

**Capture**:
采集。lab 从一批机器上取回并归一化的原始素材：身份登记表、各机器的日志切片、控制面与
数据库快照、截图、合并时间线、连接与时钟记录。**它不含任何判定**——一次采集不红也不绿，
判定是加在它上面的一层。Evidence 与 Observation 都是一次 capture 加上各自的那一层，所以
两者的素材布局字面相同，一起演进。
_Avoid_: 证据、素材、artifact、把它说成 evidence 的别名、在它里面放判定

**Evidence**:
一次 run 的 capture 加上判据结论。只增不改，红的不允许被绿的覆盖。一段 watch 的产物不是
evidence，见 Observation。
_Avoid_: 报告、artifact、日志、把素材与判定混为一谈

**Watch**:
一段持续观察。lab 盯着一套已经在运行的被测系统，校验不变量、执行修复。没有终点，
不跑场景，不摆装置，不判红绿。**它不为了观察而起停任何进程——但它的 Repair 可以**：
起停是修复的一种手段，不是观察的一种手段。与 Run 并列，共用机器、角色与适配层。
它分两层：控制端的编排，加每台被观察机器上的 Probe。
_Avoid_: 监控模式、daemon、把它说成一种 run、把 repair 的起停算成 watch 的起停

**Observation**:
一段 watch 的 capture 加上检出的不变量违反、做过的修复及其账。与 Evidence 并列。
_Avoid_: 监控数据、日志、把它归到 evidence 名下、把素材与判定混为一谈

**Probe**:
探针。lab 部署在被观察机器上、在那台机器本地执行检测与修复的那半边。它存在的唯一理由是
**延迟**——有些反应窗口比一次到控制端的往返还短，那种窗口只能在机器上赢。**Run 与 Watch 都
可以有探针**：那条理由是拓扑的性质，与哪种模式无关，而 Run 一样跨机——一次跑场景时要抢的窗口
和一段观察里要抢的是同一种。它是**一次 run 或一段 watch 作用域**的：开始时部署、结束时清除，
从不常驻。它带着一份有额度、有有效期的授权离线判定，所以控制端联系不上它的时候它照常工作，
事件攒在本机、重连后补传。控制端连不上探针叫**回传中断**，探针连不上被测系统才叫**失明**
——两者混为一谈就会把「网不好」说成「车出事了」。
_Avoid_: agent、daemon、常驻服务、把它说成一个角色、把回传中断说成失明、拿这个词去叫一次性的读取
（那个动作叫 read）、把它说成只有 watch 才有

**Sample ring**:
采样环。探针上那一份不断被刷新的快照：每个要读的东西按自己的节奏采一次存进来，判据只读它。
它存在是因为四件本来各自为难的事在它上面同时成立——判据不必联网就能求值、判定的耗时上界与
读取的耗时无关、「从上次问到现在」有了那段可问的历史、而把它的输入换成一份归档的素材就是重放。
读取的重试发生在采环这一侧（一次读不到是噪音，连着读不到才是故障），求值那一侧撞到时限就
交出「判不了」，两者因此不再互相牵扯。**采得比判得多是对的，但采而不判的部分绝不参与判定**，
它只进账。
_Avoid_: 缓存、快照池、把它说成一次采集、让判据绕过它去现读、拿采而不判的东西下判断

**Control plane**:
被测进程为自动化专门暴露的读写入口，例如一个 loopback HTTP 端点或一组 UI 控件。
lab 只经控制面读状态、注入故障，从不绕过被测方摆终态。watch 只用它的读侧。
_Avoid_: 测试接口、后门、API、把旁听面说成控制面

**Tap**:
旁听面。lab 对一个自己管不着的东西所做的只读读取，读的是那东西本来就在提供的接口，不是为测试
开的面。与控制面的区别有两条，都承重：控制面是**被测进程为 lab 开的**，旁听面是**lab 自己凑
过去听的**；控制面有写侧，**旁听面只有读侧**——内核对它一概不暴露写，要写就不再是旁听。它存在
是因为有些东西 lab 必须看见却无权指挥：一台第三方 IO 模块的寄存器里有被测系统正在依据的事实，
而那台模块不是被测进程，它的接口也不是谁为测试开的。读它读到的是**位，不是含义**——位代表什么
由被测系统的代码或现场的冻结决定，旁听面只负责把位与时刻记下来。
_Avoid_: 旁路（在被测系统里已是「绕过安全」）、控制面、后门、镜像、给它加写侧、在旁听面上解释
语义

**Rig**:
装置。适配层声明的一组角色的命名组合及其默认配置，例如「全替身」、「真车载端加真
模拟器」、「去掉模拟器接真 IO 模块」。场景只说自己要哪个 rig，不自己挑角色。一个 rig 也说明
哪些角色在它这里**缺席**，而缺席的表达就是不出现在它的角色清单里——一个缺席的角色在机器上跑
着，这套装置就是不干净的，因为「哪个才是真的那一个」变成了排查时要先问一遍的问题。它与机器的
tier 正交：tier 说一台机器归谁管，rig 说一套装置由什么组成；两者互不决定，但不是每个组合都
在物理上存在。
_Avoid_: 环境、profile、mode、用布尔开关代替、与 tier 混为一谈、把缺席说成一种角色

**Hook**:
钩子。适配层为一个角色实现的一个生命周期动作：发布、落地、配置、启动、判就绪、停止、快照。
由 lab 在对的机器上调用，输入输出都是 JSON。
_Avoid_: 脚本、回调、生命周期方法

**Pin**:
钉。一个角色声明的「本次跑的是被测系统的哪一份代码」：源仓库加一个 commit。它必须在那个仓库的
**origin 上可达**——只在某台机器本地存在的 commit 不构成身份，别人拿着证据复现不出来。钉的对象是
代码不是产物，所以证据里另记两样：产物的哈希，在 commit 因为合并而变得不可达之后它还对得上；以及
**从机器上读回来的那个 commit**，它说的是机器上实际跑着什么，而它与钉的可以不一样。被测系统在配置
里自称的 commit 是一条待核的声明，不是它。
_Avoid_: 版本、tag、build、把配置里自称的 commit 当成它、把「钉在某个分支上」当成钉

**Toolkit**:
工具包。lab 自己要用、但不是 lab 写的那些第三方程序——驱动界面的那个命令行工具、跑测试的那个框架。
它与被测系统的产物是两件事：产物由被测系统的源码构建出来，身份是一个 commit（见 Pin）；工具包从上游
取来，**身份是一个哈希**。**二进制不进版本库，进版本库的是重新取回它所需的一切**——版本、每个文件的
哈希、上游地址、许可证。到一台机器的路只有一条：在能上网的那台机器上取回并校验，经 lab 自己的传输送
过去，落地再校验一次。它按哈希缓存而不是按版本号，所以升级是并存不是覆盖，退回去只是改一行清单。
换版本要不要先上机器重测一遍，判据是**这个工具坏掉的时候会不会不出声**——一个静默给出错答案的工具
必须重测，一个坏了就报错的不必。
_Avoid_: 依赖、vendored（在被测系统里已是「抄一份定义并用测试锁住」）、Toolchain（已被 .NET 构建
基线占）、把它与被测产物混为一谈、把版本号当成它的身份、按「重不重要」决定要不要重测

**Footprint**:
足迹。lab 在一台机器上留下的东西。**它不是一份写好的清单，是一次对账**——产生足迹的每段代码在自己
旁边声明它碰什么，命令跑的时候把那份声明与机器上实际读回来的东西比一遍，三格里最值得看的是
**机器上有、却没有人声明**的那一格。一份集中的手写清单办不到这件事，因为那是一份总有人忘记更新的
清单。足迹按两维分，而这两维不是描述是判定输入：**这是谁的东西**——只有 lab 用的，删了只影响 lab；
被测系统在用的，删了被测系统会变，于是它自动落进安全门那一档，不需要另写一条按机器分的规则。
**它会不会自己消失**——会自己消失的不需要任何人记得它，只有会积累的那些才需要一条淘汰规则。
**清理是一个动作，所以它必须能被核销**：删完要读回来确认真的没了，因为「删了」在这些机器上的失败
模式是静默的。一样东西**故意**留下与**忘了**清掉，在机器上看起来完全一样，所以「故意」这件事必须
写在声明里而不是留在谁的记忆里。
_Avoid_: 清理脚本、垃圾回收、把它写成一份集中的手写清单、按机器分严格性等级（那是第二道门）、
把「发出了删除命令」当成「删掉了」、把别人留在这台机器上的东西算进 lab 的足迹

**Verb**:
lab CLI 暴露给 agent 的一个动作。分两种：内核动词对任何适配层都成立，例如起某个角色、
等某条判据；具名动词由适配层注册，带被测系统自己的含义，例如注入某种故障。每个动词声明
四样东西。三样喂给安全门：它对物理世界的影响程度、它可不可逆、它的前置条件；第四样在动作
之后才用得上，是**它做成了没有的判据**——执行完靠它判结果，断连恢复后靠它判「到底做了没有」。
四样都是判定的依据，不是权限开关，而**不声明不等于放行**——一个会碰物理世界却没声明前置
条件、或者没说清怎么算做完了的动词，内核直接拒绝。影响程度与可逆性是两回事：一个不碰物理
世界的动作照样可以完全撤不回来。
_Avoid_: 命令、工具、function、把影响程度当成权限等级、把可逆性算进影响程度、把「做成了没有」当成执行的返回码

**Predicate**:
判据。一个对被测系统状态的命题，写成一份纯数据而不是一段代码——因为它要能被送到一台远端
机器上、在那里没有控制端也能求值，还要能在一份归档的素材上重放。它读的是采样环里已经采好
的快照，**自己从不发起读**，所以求值是一个纯函数、有确定的耗时上界。判据本身不带时间：
「此刻成不成立」由它答，「从上次问到现在有没有不成立过」由包在它外面的时间算子答。断言、
不变量、前置条件与「做成了没有」的判据是同一个判据配不同的时间算子。它有四个结果：成立、
不成立、**判不了**、**不适用**——后两个不能合并，一个是这套装置下没有这回事、根本不该问，
另一个是该问但此刻读不到，前者不进结论，后者不算通过。
_Avoid_: 表达式、脚本、规则、条件、把「判不了」与「不适用」合成一个值、让它自己去读

**Assertion**:
断言。场景在某一时点对被测系统状态的判定，成立与否决定一次 run 的红绿。它就是一个判据配
上「此刻」这个时间算子。与不变量共用求值器与四个结果，但**读法不同**：场景有终点，所以
断言判不了就算失败——等不到就是没发生；一段观察没有终点，判不了只是判不了。
_Avoid_: 检查、expect、与不变量混用、以为共用求值器就等于共用结局

**Progress signal**:
进度信号。适配层声明的一个单调不减的读数，说明被测系统「又有过一次机会」。它存在
是为了让「它没有做某件事」可以被等待——等信号涨够，再断言什么都没发生。内核只知道
读一个数、等它涨够，不知道那个数是什么。一套装置没有这样的读数就显式声明为空，
此时等待直接失败，而不是退化成等一会儿。
_Avoid_: 心跳、tick、计数器、用等待时间代替它

**Invariant**:
不变量。一条在被测系统正常运行期间必须始终为真的命题。与断言共用判据，区别在时间：
断言问「此刻成不成立」，不变量问「从上次问到现在有没有不成立过」。内核声明跨被测系统
都成立的那几条——进程还在不在、端口还听不听、控制面还答不答、探针还看不看得见被测系统、
进度信号还涨不涨——语义的那些由适配层声明。**一条不变量知道自己在哪些装置下不适用**，
而那与「此刻判不了」是两回事：不适用的那条根本不会被加载，因此不会拖住整体结论。它还
声明自己多久问一次，而**问得比一秒一次更勤要说出理由**：节奏是从「要赢多宽的窗口」推出来
的，不是从「多久算及时」推出来的——后者是主观词。
_Avoid_: 规则、告警、alert、把它说成断言、把「不适用」当成「判不了」、不说理由就问得很勤

**Violation**:
违反。一条不变量从成立转为不成立的那一段，从第一次不成立开始，到重新成立为止。**它是一段
而不是一个时刻**，因为要修它、要数修了几次、要在修不好时放弃它，这些都得有一个东西可以指。
两条违反是不是同一条，看三样：哪条不变量、哪套被测系统的哪一部分、这一段从什么时候开始——
三台车的同一条不变量是三条，同一台车相隔两次的同一个毛病也是两条，否则一整夜的断续故障会
在第三次修复失败后被当成一件事永久放弃。中间转成「判不了」不算结束——读不到不是好了。
_Avoid_: 告警、故障、事件、把它当成一个时刻、按不变量名一条到底、让判不了闭合它

**Repair**:
修复。watch 期间对被测系统做的、意在让它回到正常的写操作。能不能执行由安全门判定，
做过什么必须进账。**「不动」也是一种修复决定**，与被安全门拦下是两回事，同样进账——
一次真车上的先例里，旅程走到终态 Blocked 时正确的动作恰恰是不停服务，因为停了就把恢复
所需的那个循环拿走了。修复用的就是动词，**没有另一类专供修复的动作，也没有一份「只能修
这几样」的清单**——挡住危险动作的是安全门，不是名单。**执行完还没结束**：一次报了成功却
没有生效的修复，比一次干脆失败的更危险，因为账上写着修好了而人会信它，所以结果与验证分开
记。同一件事反复修不好时，放弃的是**那一条违反**而不是整段观察——观察继续，只是不再对它
动手，而重新动手需要人回来授权。
_Avoid_: 自愈、自动恢复、把一次修复说成一次 run、把「不动」当成没有决定、为修复另立一套动作、把执行成功当成修好了

**Diagnostic scenario**:
只读诊断场景。watch 检出异常后唯一可以自动拉起的那类场景：不起停任何进程、不注入故障、
不写被测系统的任何东西，只多采一批证据。它是场景的一种，所以它的一次执行仍然是一次 run、
仍然产出 evidence，观察那边只留一个指向它的记号。**它由适配层申报，不是推断出来的**，而
申报是不是真的只读只能在运行中发现——一旦用到会动被测系统的动词就当场拒绝。它存在的理由
是证据可信而不是安全：一次完整的 run 会起停进程、注入故障，那会破坏它正在诊断的现场，而
卡住那一刻的完整快照是排查生产故障唯一不可再生的东西。同一条异常反复触发它也不行，理由
一样——诊断本身会把要看的现场淹掉。
_Avoid_: 健康检查、探活、把它说成一次 watch、指望静态推出它是不是只读

**Safety gate**:
安全门。一个纯判定：给定一个待执行的动作、被测系统此刻的状态、一份 Grant，输出这个动作
此刻该不该做。它不执行任何东西，也不知道执行的结果。Watch 的每次 Repair 之前、Run 的每个
动作之前过的是同一道门，两侧只差 Grant 的来源与寿命。**判不出就拒绝**——读不到状态、判据
求值出错、判定超时都算判不出，而「判不出」既不是安全也不是不安全。它**必须能在被观察机器
上离线完成**：有些反应窗口比一次回控制端的往返还短，一道要回去问一次的门赢不了那种窗口。
注意被测系统自己也有一道叫「安全闸门」的东西（车在动就拦、停稳才放行），那是它的业务规则，
与这道门无关。
_Avoid_: 权限、白名单、审批、与被测系统自己的出发闸门混为一谈、把它做成一次询问

**Grant**:
授权。人给一段 watch 或一次 run 的一份额度：允许到哪一级影响程度、能用几次、什么时候到期、
谁给的。它是安全门的输入之一，探针带着它离线判定，所以它必须是一份**能随探针走的东西**，
而不是一次可以回头再问的询问。到期看两个钟——一个墙上的、一个只会往前走的——哪个先到算
哪个，因为两者各有一种失效方式：墙上的钟会偏，往前走的那个在探针重建后归零。
_Avoid_: 权限、token、把它说成一次确认、只用一个钟判到期

**Precondition**:
前置条件。适配层为一个动词声明的一组命题，说明什么情况下这个动作才**该**做。它与不变量共用
表达形式，因为两者都要能被送到一台远端机器上离线求值。它答的是**时机**，不是危险程度——
一次真车上的先例里，旅程走到终态 Blocked 时不停服务才是对的，理由不是停服务危险，而是恢复
正需要那个会被停掉的循环。任何一条不成立，这次动作的结局是「不动」，与被安全门拦下是两回事。
_Avoid_: 断言、守卫、把它说成权限检查、与不变量另立一套表达、把「不动」与「被拦下」混为一谈

**Ledger**:
账。**每一次「要不要动被测系统」的判定**留下的一条人可读的流水：何时、对哪台机器的哪个
角色、做了什么、为什么、结果如何。记的是判定而不是写操作——被拦下的、判断出此刻不该动的，
都没有写操作，而它们恰恰最值得留痕：一次被拦下的危险动作比一次成功的修复更说明问题。谁
授权的这段观察写在开头，不在每一条里重复。读者是人，读它不需要 agent 帮忙解释；它是
observation 的一部分，不是一个界面。**它是渲染出来的**——权威数据是机读的那一份，账随时
可以整份重建，所以「人读的」这个身份不必牺牲完整性。**写下来没人看与没写没有区别**——把其中该让人
知道的那几段推到人眼前是另一件事，见 Courier。**它挡不住篡改**：任何能在那台机器上
写文件的东西都能改它，包括写它的那个 agent 自己。这一条不加掩饰地写在接入文档里。
_Avoid_: 审计日志、log、做成页面、只记执行成功的那些、把它当成不可篡改的证据、在每行重复授权信息

**Courier**:
送达。把那十类「必须让人知道」的事从账里推到人眼前的那个部件。**它不产生内容**——送的就是账里那一段
的原文，所以同一件事不会在两处漂移。它只在控制端上跑，探针一行都不加：不是因为车上没有出网的路
（车有，实测过，缺的只是一个能用的 DNS），而是因为**能对外发消息的凭据不下车**。它送出去之后知道的
只有「通道收下了」，**不是「人知道了」**——群机器人没有已读回执，所以账里写的是前者；把它写成后者
就是又一扇开着的门加一个相信它的人。送不出去时事件照旧成立、账照旧记，攒着等通路回来，因为传输的
失败与被测系统的判定是两类事。去重靠**围栏**——同一身份的新事件取代还没发出去的旧的，理由是发一条
过时的状态比不发更糟——而不靠「多久内最多几条」那种阈值，阈值要调，围栏不用。
_Avoid_: 通知系统、告警、Notification、Delivery（协议层「消息投递」占着）、Alert（被测系统的
`EXCEPTION_RAISE_ALERT` 占着）、把它说成一个界面、把「通道已收」说成「已送达」

**Vigil**:
守望。**在控制端之外**等着控制端出声的那个东西——Courier 是「发」，它是「等」。它存在的唯一理由是
一条只在异常时才出声的通路无法证明自己还活着，而**缺席要成为信号，就必须有东西在等**。它等的不是
一条专门的心跳，而是「这段 watch 最近一次真的写下了什么」的时刻，所以编排卡死就等不到，不需要另外
去检测「卡住」。它跑在一台**不是控制端**的机器上，且**不加载 lab 的任何模块**——依赖 lab 就意味着
lab 的一次坏改动会同时干掉被看的和看着的。它只有嘴没有手：能发消息，不能改任何被测系统的状态。
它说的永远是**「我听不到控制端」而不是「控制端挂了」**，因为它区分不了这两件事，而假装能区分就是
又一扇开着的门。**没有东西在等它**，递归就在这里止住——它自己每天无条件出声一次，剩下的交给人。
_Avoid_: Watchdog（会被读成 Watch 的一部分，而它恰恰在 Watch 之外）、Heartbeat（被测系统重度占用）、
监控、告警、把它说成 Watch 的一层、让它去改被测系统、说它能证明控制端挂了
